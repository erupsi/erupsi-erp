/**
 * Returns the sum of two numbers.
 * @param {number} a
 * @param {number} b
 * @return {number}
 */
function sum(a, b) {
    return a + b;
}

module.exports = sum;
// (Removed redundant comments)
